apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort  # ou LoadBalancer si cloud
  selector:
    app: frontend-service
  ports:
    - name: http
      port: 7000
      targetPort: 7000
      nodePort: 30000  # accessible via http://<NODE_IP>:30000
